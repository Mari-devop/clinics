import styled from "styled-components";

export const FilterButtons = styled.div`
    display: flex;
    flex-direction: row;
    margin: 10px 50px;
    gap: 60px;
    label {
       padding-left: 5px;
       font-size: 14px;
       color: var(--secondary-color);
       font-weight: 500;
    }
    `;